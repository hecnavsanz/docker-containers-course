apiVersion: v1
kind: Pod
metadata:
  name: stress-cpu
  namespace: default
spec:
  containers:
  - name: stress-cpu
    image: vish/stress
    resources:
      limits:
        cpu: "1"
      requests:
        cpu: "0.5"
    args:
    - -cpus
    - "2"